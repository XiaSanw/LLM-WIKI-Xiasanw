# 迭代报告

## 迭代 3 — 2026-05-24

### 完成事项

**第二批基建**
- 最小 API 服务：GET/PUT /pages + POST /search，零依赖单文件
- source-registry：10 种来源类型路由（含图片）
- adapter-state：5 状态来源检测

**Agent 写作质量改造**
- schema 深度自审清单取代"最低标准"——写作目标从"1500 字"变成"读者能决策"
- agent.md ingest 改三阶段：榨干素材 → 深度写作+自审 → 收尾验证
- 全部 12 页标题中文化 + 英文副标题

**内容与架构**
- 去域化：kb/ai/ → kb/，标签+链接组织
- 新增 projects/ 页面类型 + 目录
- 新增 about-me.md 用户档案
- 新增图片素材支持（raw/images/ + source-registry）
- Inbox 扫描逻辑（~/inbox/ → "扫一下 inbox"）
- Claude Code 700→5871 字，Letta 1463→5975 字
- CPA 1500→10943 字
- git init，首次 commit

### 当前状态

```
kb/
├── about-me.md           ← 用户档案
├── .wiki-schema.md       ← v2.0 深度自审清单
├── index.md              ← 内容索引（含浏览指引）
├── log.md
├── raw/ (3 篇 + images/)
└── wiki/
    ├── entities/ (8 篇)   ← 全部中文化，2 篇已深度展开
    ├── topics/ (2 篇)
    ├── sources/ (2 篇)
    ├── projects/ (空)
    ├── comparisons/ (空)
    └── synthesis/ (空)
```

### Lint 状态

| 问题 | 数量 | 状态 |
|---|---|---|
| schema 合规 | 0 | ✓ |
| 断链 | 0 | ✓ |
| stale 页面 | 0 | ✓ |
| sources 为空 | 6 | ⚠ 分析类页面正常 |
| 内容深度不足 | 0 | ✓ |

**wiki 页面总数：12 篇**（entity 8 + topic 2 + source 2）

### 下一步

1. 填 about-me 真实内容
2. 丢真实素材进来，触发首次 comparison（3+ 可比实体已满足条件）
3. 部署 Quartz 到公网
4. FTS5 全文检索（wiki 到 50+ 页时）

---

## 迭代 2 — 2026-05-12

### 完成事项

**Schema 硬性标准化（学习参考系统）**
- 实体页最低 1500 字符、"不同素材中的观点"必填、禁止占位文本
- 素材摘要最低标准：实操内容保留 + 至少 2 段原文摘录 + 40% 信息保留率
- 主题页最低标准：至少 5 条核心观点，每条标注来源，知识体系 + 综合分析必填
- 新增内容分级处理规则：>1000 字完整处理，<=1000 字简化处理
- 新增自动触发规则：3+ 可比实体 → comparison，5+ 素材 → synthesis
- 强化交叉引用规范：禁止悬空链接，"链接名=文件名"铁律

**领域运维文件**
- 创建 `kb/ai/index.md` — 内容索引，按类型分类的完整目录
- 创建 `kb/ai/log.md` — 操作日志，按时间线记录所有变更
- 更新 `agent.md` — 新会话启动流程加入 index.md + log.md

### 当前状态

```
kb/ai/
├── .wiki-schema.md    ← v2.0（含硬性数字标准）
├── index.md           ← 新建
├── log.md             ← 新建
├── raw/ (1 篇)
└── wiki/
    ├── entities/ (6 篇)
    └── topics/ (2 篇)
```

### 下一步

1. **继续 ingest** → 收录更多 AI 领域素材，触发自动综合生成
2. **回填已有 wiki 页** → 按新 schema 标准检查现有 8 篇页面是否达标
3. **建立其他领域** → work、stocks 等领域的 schema
4. **实现 FTS5 检索** → 从 grep 升级到 SQLite FTS5 全文索引
5. **写 digest skill** → 定期生成"本周值得精读"摘要

---

## 迭代 1 — 2026-05-05 ~ 2026-05-06

### 完成事项

**方向校准**
- 从"个人 wiki 搜索系统"重新定位为"Agent 无关的个人知识平台"
- 确立核心动机：让 AI 和你共享同一段知识记忆，跨会话积累，不绑定任何 Agent 工具
- 确立"一份知识，三个读者"模型：你浏览 / Agent 注入上下文 / RAG 检索细节

**Schema 设计**
- AI 领域 `.wiki-schema.md` 完成，定义 5 种页面类型：
  - entity（实体）| topic（主题）| source（素材摘要）| comparison（对比）| synthesis（综合分析）
- 每种类型有明确模板、质量标准、confidence 标签体系

**Skill 体系**
- `/llmwiki-ingest` — 素材消化：读 schema → 去重 → 存 raw → 搜索已有页 → 新建/更新 → 反向链接 → 报告
- `/llmwiki-query` — 知识检索：三层策略（精确匹配 → wiki 全文 → RAG 兜底）+ Agent 上下文注入模式
- `/llmwiki-lint` — 质量审计：检查 schema 合规性、内容质量、死链接、时效性

**知识积累**
- 收录 1 个 entity：Claude Code
- 收录 1 个 entity + 1 个 topic：Fashion AI 项目 + AI 电商图像生成模式
- Raw 原文存储验证通过

### 当前状态

```
llmwiki-base/
├── README.md              ← 系统设计文档（已更新）
├── 迭代报告.md             ← 本文件
├── agent.md               ← 新 Agent 入口文件
└── kb/
    └── ai/
        ├── .wiki-schema.md
        ├── raw/ (1 篇原文)
        └── wiki/
            ├── entities/ (2 篇)
            └── topics/ (1 篇)
```

### Lint 结果（上次运行: 2026-05-12）

| 问题 | 数量 | 状态 |
|---|---|---|
| schema 合规 | 0 | ✓ |
| 内容质量 | 0 | ✓ |
| 死链接 | 0 | ✓ |
| stale 页面 | 0 | ✓ |

**wiki 页面总数：8 篇**（entity 6 + topic 2）

### 待回填项

现有 8 篇 wiki 页面在 Schema v1.0 时期创建，需对照 v2.0 标准回填：
- 实体页 sources 字段检查
- 实体页字数检查（是否达 1500 字符）
- 摘要页信息保留率检查（40% 目标）
- 禁止悬空链接检查（`[[待创建 xx]]` 格式需改为纯文本）

---

## 版本记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-05-24 | v0.3 | 第二批基建 + Agent 写作改造 + 去域化 + git init + 图片支持 |
| 2026-05-12 | v0.2 | Schema 硬性标准化 (v2.0)，新增 index.md + log.md |
| 2026-05-06 | v0.1 | 项目方向校准，AI 领域 Schema + 3 Skill + 3 篇 wiki |
