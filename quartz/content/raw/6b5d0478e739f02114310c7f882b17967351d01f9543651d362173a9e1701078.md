# Fashion AI Promo Photo Pipeline

来源: /Users/xiasanw/Nutstore Files/ai/reference/Fashion-AI (本地项目, git clone)
GitHub: https://github.com/liangdabiao/Fashion-AI.git
采集时间: 2026-05-05

---

电商 AI 生图爆款流水线 — 输入新品平铺图，自动检索相似爆款、分析风格，生成专业宣传图。

## 项目简介

跨境电商场景中，商家每出一款新品都需要制作宣传图（模特穿着商品的照片）。传统方式需要请模特、搭场景、拍摄修图，成本高、周期长。

本项目实现了一套全自动的 AI 生图流水线：

1. **以图搜爆款** — 将新品平铺图与历史爆款库做混合检索（视觉相似 + 关键词匹配 + 销量过滤），找到风格最接近的爆款
2. **AI 分析风格** — 将爆款参考图发给大语言模型，自动分析场景、灯光、姿势、氛围，生成风格描述 prompt
3. **AI 生成宣传图** — 将新品图 + 爆款参考 + 风格描述一起发给图像生成模型，输出专业宣传图


项目灵感来源于教程：Nano Banana2+ Milvus+ qwen3.5，打造电商生图爆款流水线
文章参考来源：https://mp.weixin.qq.com/s/itMVbtZDW0IzIBhp6e9OHw

### 为什么用向量数据库？

电商最核心的资产是大量经过市场验证的爆款图。模特表现力、场景构图、光影质感，都是花钱试出来的。与其每次从零写 prompt，不如直接从历史爆款中检索最相似的视觉特征，让 AI 继承"爆款基因"。这正是 Milvus 混合检索发挥作用的地方。

### 项目简单使用：

  使用方式：
  1. 将真实商品图片放入 images/（替换占位图），新品放入 new_products/
  2. python main.py setup — 建库 + 编码 + 入库
  3. python main.py search --new-id NEW001 — 测试检索
  4. python main.py generate --new-id NEW001 — 完整生图流水线

### 为什么用混合检索（Dense + Sparse + Scalar）？

单一检索方式有盲区：

| 检索方式 | 擅长 | 弱点 |
|---------|------|------|
| Dense 向量（图片 Embedding） | 视觉相似度（颜色、款式、风格） | 不擅长精确关键词匹配 |
| Sparse 向量（TF-IDF） | 关键词语义（"floral"、"midi"、"chiffon"） | 不理解视觉特征 |
| 标量筛选 | 品类过滤、销量排序 | 无法理解语义 |

三者结合 + RRF（Reciprocal Rank Fusion）重排序，检索精度远超单一方式。

## 技术架构

新品平铺图 → llama-nemotron-embed-vl-1b-v2 (Embedding) → Milvus 混合检索 (Dense+Sparse+Scalar+RRF) → Top-K 相似爆款 → Qwen 3.5 多模态分析风格 → Gemini 3.1 Flash 生成宣传图 → output/

### 使用的模型

所有模型通过 OpenRouter API 调用，无需本地 GPU。

| 模型 | 用途 |
|------|------|
| nvidia/llama-nemotron-embed-vl-1b-v2 | 图片/文本 Embedding, 免费, 2048维 |
| qwen/qwen3.5-397b-a17b | 爆款风格分析, 多模态 |
| google/gemini-3.1-flash-image-preview (Nano Banana 2) | 默认生图, ~$0.067/张 |
| openai/gpt-5.4-image-2 | 备选生图, 人像融合好 |
| openai/gpt-5-image (Pro) | 高质量生图 |
| openai/gpt-5-image-mini | 低成本生图 |

### Milvus 混合 Schema

Collection: fashion_products
- id INT64 (主键)
- product_id VARCHAR
- category VARCHAR (maxi_dress, midi_dress, casual_top)
- color VARCHAR
- style VARCHAR (bohemian, floral, bodycon, ...)
- season VARCHAR
- sales_count INT64 (用于标量过滤)
- description VARCHAR (用于 TF-IDF)
- price FLOAT
- dense_vector FLOAT_VECTOR(2048)
- sparse_vector SPARSE_FLOAT_VECTOR

## 项目结构

cloth-milvus/
├── main.py              # CLI 入口 (setup / search / generate)
├── config.py            # 配置管理
├── utils.py             # 工具函数 (图片编解码)
├── embeddings.py        # Dense/Sparse 向量生成
├── milvus_store.py      # Milvus 建库、插入、混合检索
├── style_analyzer.py    # Qwen3.5 风格分析
├── image_generator.py   # 多模型生图
├── data_setup.py        # 示例数据生成
├── requirements.txt
├── images/              # 历史爆款商品图片
├── new_products/        # 新品平铺图
├── products.csv         # 历史商品元数据 (40条)
├── new_products.csv     # 新品元数据 (4条)
└── output/              # 生成的宣传图

## 关键技术点

1. 混合检索: Dense + Sparse 两路并行检索, RRF 融合排序
2. 多生图模型: 统一 OpenRouter API 调用, 4 种模型可切换
3. Qwen 3.x Thinking 模式: 通过 enable_thinking: False 关闭

## 成本参考

- Embedding (nvidia): 免费
- Qwen 3.5: ~$0.001/次
- Gemini 3.1 Flash: ~$0.067/张
- 单次处理: 约 $0.07/件

## 已知限制

- 占位图无法用于实际检索和生图
- 图片分辨率建议 >= 800x800
- Nano Banana 2 服装-人体融合偶尔不自然
- Milvus Lite 不支持 Windows, 使用 Zilliz Cloud 替代

## 依赖

pymilvus>=2.4.0, openai>=1.0.0, requests>=2.31.0, pillow>=10.0.0, scikit-learn>=1.3.0, tqdm>=4.65.0, python-dotenv>=1.0.0, numpy>=1.24.0

## 参考

项目灵感来自微信文章: Nano Banana2+ Milvus+ qwen3.5，打造电商生图爆款流水线
https://mp.weixin.qq.com/s/itMVbtZDW0IzIBhp6e9OHw
特别感谢 linux.do 社区, 佬友 trader 提供 API 支持
